rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // En producción, solo los usuarios autenticados pueden leer o escribir en el Storage.
      allow read, write: if request.auth != null;
    }
  }
}
